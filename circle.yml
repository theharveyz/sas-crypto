machine:
  node:
    version: 4.4.5
test:
  pre:
    - make pre-build
    - npm install
    - npm run lint
  post:
    - ./node_modules/.bin/nyc report --reporter=text-lcov > ./coverage/lcov.info
    - bash <(curl -s https://codecov.io/bash)
